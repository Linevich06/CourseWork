<Window x:Class="LinearRacingGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:winForms="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        Title="Линейные гонки" 
        Width="800" 
        Height="600"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <!-- WindowsFormsHost для размещения DX-рендеринга -->
        <WindowsFormsHost x:Name="DxHost">
            <winForms:Control x:Name="RenderControl"/>
        </WindowsFormsHost>

        <!-- Оверлей для UI элементов -->
        <Grid>
            <TextBlock Text="Игрок 1: WASD" 
                       HorizontalAlignment="Left" 
                       VerticalAlignment="Top"
                       Margin="10"
                       Foreground="White"
                       FontSize="16"/>

            <TextBlock Text="Игрок 2: Стрелки" 
                       HorizontalAlignment="Left" 
                       VerticalAlignment="Top"
                       Margin="10,40,0,0"
                       Foreground="White"
                       FontSize="16"/>

            <Button x:Name="StartButton"
                    Content="Старт"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,0,20"
                    Width="100"/>
        </Grid>
    </Grid>
</Window>